entero gClave
cadena gNombre
entero i

def capturarDatos()
    salida("Ingrese la Clave:")
    entrada(gClave)

    salida("Ingrese el Nombre:")
    entrada(gNombre)
findef

def imprimirDatos()
    salida("-> Datos guardados: ", gClave, gNombre)
findef

inicio
    salida("--- SISTEMA DE REGISTRO (3 PERSONAS) ---")

    # Inicializamos el contador manualmente
    i = 1

    # Usamos MIENTRAS en lugar de CICLO
    mientras i <= 5
        salida("-----------------------------")
        salida("Registro Numero:", i)

        capturarDatos()
        imprimirDatos()

        # Incrementamos manualmente
        i = i + 1
    finmi

    salida("-----------------------------")
    salida("Proceso terminado.")
final