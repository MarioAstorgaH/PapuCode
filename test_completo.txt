entero n
entero i
entero resultado

inicio
    salida("================================")
    salida("   CALCULADORA DE FACTORIAL     ")
    salida("================================")

    salida("Ingresa un numero positivo:")
    entrada(numero)

    # Probamos el SI y el SINO
    si numero < 0
        salida("ERROR: No se puede calcular factorial de negativos.")
    sino
        resultado = 1
        i = 1

        # Probamos el MIENTRAS (Ciclo)
        mientras i <= numero
            resultado = resultado * i
            i = i + 1
        finmi

        salida("El factorial de", numero)
        salida("es:", resultado)
    finsi

    salida("================================")
    salida("Fin del proceso.")
final